/** Public brand marks — served from `/public/brand`. */
export const BRAND_LOGO = {
  /** White line-art emblem on black square — masked to black on light surfaces via BrandLogo */
  mark: '/brand/logo.png',
  /** Natural aspect ratio of the mark (square) */
  aspectRatio: 1,
} as const
