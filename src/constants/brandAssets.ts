/** Public brand marks — served from `/public/brand`. */
export const BRAND_LOGO = {
  /** Die-cut fly sticker (black fly, white outline, black field) — same asset on all surfaces */
  mark: '/brand/logo.png',
  /** Natural aspect ratio of the mark (square) */
  aspectRatio: 1,
} as const
